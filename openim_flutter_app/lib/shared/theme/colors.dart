import 'package:flutter/material.dart';

/// 全局颜色配置 - 温暖希望·绿色行动
class AppColors {
  // 主色与辅助色
  static const Color primary = Color(0xFF2E7D32); // 深森林绿
  static const Color accent = Color(0xFFFF9800); // 活力橙

  // 状态色
  static const Color success = Color(0xFF4CAF50);
  static const Color danger = Color(0xFFF44336);
  static const Color disabled = Color(0xFFBDBDBD); // 统一禁用灰

  // 背景色
  static const Color pageBackground = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;

  // 文本色
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // 分割线
  static const Color divider = Color(0xFFEEEEEE);

  // 遮罩
  static const Color mask = Color.fromRGBO(0, 0, 0, 0.6);

  // 阴影色（约等于 rgba(0,0,0,0.06)）
  static const Color shadow = Color(0x0F000000);
}


